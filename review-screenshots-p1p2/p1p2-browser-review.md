# P1/P2 Browser Review

## Desktop Contributions review

The homepage Contributions section renders four curated repositories from the explicit six-repository allowlist. The audit strip shows `Source GitHub GraphQL / user:lora-sys`, `Allowlist 6 repositories`, and `Status No sync warnings`. The heading also shows `In sync`, the Aug 18, 2026 contribution snapshot date, and `4 repos / 8 PRs`. Each repository and each rendered pull request uses an external accessible GitHub link with a visible PR state and update date.

The contribution list keeps the existing curated presentation rather than automatically rendering every external contribution.

## Screenshot note

The standalone Chromium screenshots at both 1440×900 and 390×844 did not honor the `#contributions` anchor and remained at the homepage hero. This is a capture-position limitation, not a layout failure. The real browser review was already precisely scrolled to the Contributions section and showed the complete audit strip and four contribution records. A real-browser mobile scroll will be used for the final 390×844 evidence.
