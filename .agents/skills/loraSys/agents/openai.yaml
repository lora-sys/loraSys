interface:
  display_name: "LoraSys"
  short_description: "Repo-specific patterns and workflows for loraSys"
  default_prompt: "Use the loraSys repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true