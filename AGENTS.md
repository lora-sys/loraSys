# loraSys

中文优先的个人品牌与内容中心。它用 Astro 静态输出，把 Agent 项目、开发者工具、构建笔记、公开贡献、动漫与收藏整理为访客可探索的作品档案；公开站点由 GitHub Pages 托管，数据同步与发布由 GitHub Actions 完成。

这个仓库不是后台产品，也不是动态内容平台。页面应该让第一次来的访客快速知道能看什么、能点什么、点了会去哪里；技术感来自真实项目和清晰证据，不来自泛科技背景或难以理解的特效。

## 这个项目最重要的几件事

### 1. 站在访客视角写与设计

项目、文章、导航和提示文字先回答“这是什么、有什么用、下一步能看什么”，不要用“代表我如何思考”“展示我的能力”一类自述式句子。外部贡献必须保留真实来源和贡献边界，不能表现成 lora-sys 自有项目。

### 2. 静态数据有明确来源边界

`src/data/github-projects.json`、`src/data/contributions.json`、`src/data/external-contributions.json` 和 `src/data/sync-report.json` 是同步快照。项目排序、精选资格、人工摘要和本地封面映射由 `scripts/fetch-github-projects.mjs` 管理；不要手工改快照来绕过脚本规则。Notion、HTML 或其他外部文章只能先进入 `content-drafts/review/`，人工审核后才可移动到 `src/content/blog/`。

### 3. 视觉资产必须原创、真实、轻量

需要插画、贴纸、印章、工作台物件或项目封面时，直接生成与当前页面任务匹配的原创资产。统一遵循 **Lora Field Notes**：暖白纸张、石墨轮廓、Teal 功能信号、低饱和苔绿/陶土橙/靛青、手作印刷材料感和充足留白。不要复用参考站的插画、角色、构图或品牌符号；不要用泛化芯片、AI 大脑、霓虹网络代替真实内容。接入前优化为 WebP，原始图、拼图和审查截图放在仓库外。

### 4. 交互是内容导航，不是表演

每个互动必须有清楚的目标和状态变化。高频 hover、press、focus 反馈控制在 100–220ms；偶发叙事过渡控制在 240–420ms；只动画 `transform` 与 `opacity`。桌面 hover 不得是访问内容的唯一方法，移动端必须有按压或明确 CTA，键盘必须有同等的 focus 路径，`prefers-reduced-motion` 必须关闭非必要动态。不要引入 WebGL、Three.js、全页鼠标跟随、自动播放或持续循环动画。

### 5. 性能与可访问性是发布条件

首页和作品页是性能敏感页面。首屏只加载必要的图和脚本；非首屏图使用尺寸声明与懒加载；任何新交互都要有语义化元素、可见焦点、合理的 `aria-*` 说明与减少动态回退。上线前的目标是：Lighthouse 性能不低于 98，TBT 接近 0ms，CLS 不高于 0.03，并且不牺牲现有内容阅读体验。

## 小词典

- **访客**：浏览公开个人站、作品、文章或收藏的人。
- **精选作品**：带有 `featuredRank` 的人工策展项目；首页只展示前三个，作品页展示全部精选。
- **Archive**：作品页中可按需展开的完整同步目录，不等于首页精选。
- **External contribution**：外部仓库中的真实贡献，必须显示来源、项目或 PR 链接，不能伪装成自有仓库。
- **同步快照**：由 `bun run sync:github` 生成并通过校验的 `src/data/*.json` 数据。
- **审核候选**：位于 `content-drafts/review/` 的本地文章转换结果；默认不发布。
- **审查材料**：截图、视频、拼图、Lighthouse 原始报告和生成中间图。它们只能放在仓库外，不能提交。
- **Lora Field Notes**：本项目的视觉资产和互动语言：工作台、纸质档案、现场记录，而不是泛科技皮肤。

## 三种最容易伤到项目的操作

1. **按名字乱杀进程。** 不要因为进程名、路径或端口“看起来像”项目就停止它。只停止自己启动并记录过 PID 的进程，或确认端口归属确实来自当前 worktree 的进程。
2. **把开发和测试指向线上状态。** GitHub Pages、已发布快照、真实用户内容和任何共享状态都不是可写测试环境。可以读取或复制用于调试，但不要让开发服务器、清理脚本、导入工具或测试直接写回线上。
3. **把环境细节写死。** 不要在页面、组件或共享数据中硬编码 `localhost`、部署地址、临时端口、token 或机器路径。站点路径遵循 Astro 的 `BASE_URL`，外部地址只在明确的数据或配置边界中出现。

## 改动时要覆盖所有相关表面

不要只修一个你刚好点到的入口。每次改动前后都检查适用范围：

- 同一个行为是否同时出现在中文、英文、首页、作品页、文章页或移动导航。
- hover、focus、active、disabled、展开、关闭、筛选为空、加载失败和减少动态状态是否合理。
- 鼠标、键盘、触摸和窄屏访问是否都有明确路径。
- 项目、贡献、文章和社交链接是否仍保留正确目的地、来源标记与外链安全属性。
- 改动是否需要更新数据脚本、同步报告、内容说明或本文件中的共享词汇。

## 开发、同步与预览

- 安装依赖：`bun install --frozen-lockfile`。
- 本地开发：`bun run dev`。不要假设端口，读取 Astro 控制台输出的实际地址。
- 生产构建：`NODE_OPTIONS=--max-old-space-size=3072 bun run build`。
- 生产预览：先完成构建，再执行 `bun run preview --host 0.0.0.0`；同样以控制台打印的地址为准。
- GitHub 数据同步：`bun run sync:github`。它会更新贡献、项目、外部贡献、校验和同步报告；网络凭据不可用时应保留当前快照，而不是伪造新数据。
- HTML 文章导入：`bun run article:import --input /path/to/article.html --title "标题"`。输出只能进入审核候选区，不得自动移动到公开内容目录。
- 停止服务时只停止本次启动并记录过的 PID，不要使用宽泛的 `pkill` 模式。

## 测试数据和内容安全

不要用空数据伪装成真实状态，除非要测试空状态。同步数据存在于静态快照中；若要测试边界情况，优先用最小、可恢复的本地 fixture，而不是改动真实贡献、线上项目或公开文章。不要把密钥、cookies、账号信息或用户私密内容写入仓库、截图、日志或测试 fixture。

> 复制进来测试，绝不把测试写回线上。

## 如何验证

先做与改动范围相匹配的最小检查。页面、共享样式、同步脚本、数据模型或发布工作流发生变化时，除了针对性验证，还必须完成以下发布前检查：

1. `bun run check` 必须是 0 errors、0 warnings。
2. 生产构建必须通过：`NODE_OPTIONS=--max-old-space-size=3072 bun run build`。
3. 需要真实交互的改动必须在浏览器中检查桌面、移动、键盘和减少动态模式；不要只依赖静态代码阅读。
4. 首页或作品页增加图片、脚本、滚动互动时，复测 Lighthouse 性能和无障碍。目标见“性能与可访问性”。
5. 生成资产必须先在仓库外做轻量通过/不通过审查，再转为 WebP 接入。

异步交互要等待真实的状态完成或 DOM 状态变化，不能用任意 `sleep` 让测试看上去通过。

## Git、PR 与发布

- 不要主动创建 PR。除非维护者明确要求，否则在当前分支完成本地验证即可。
- 提交使用 Conventional Commit，例如 `feat(home): add field-notes interactions`。
- 一次提交只处理一个清楚的主题；如果需要“顺便”改无关内容，先记录为下一步工作。
- UI 修改需要仓库外的前后对比截图；拖拽、时序或动效需要仓库外的短视频或可重复的浏览器验证记录。它们不能进入 Git。
- 只有维护者明确说“推送”“合并”或“部署”时，才推送 `main` 或触发公开发布。
- `.github/workflows/deploy.yml` 的真实顺序是：同步与质量门 → 验证构建 → GitHub Pages 部署。不要跳过同步校验，也不要让构建使用未经验证的快照。

## 系统如何工作

Astro 负责把 `src/pages/` 和 `src/layouts/` 渲染为静态页面。`src/components/` 提供可复用的页面模块和渐进增强的客户端交互；`src/data/` 保存经过校验的静态快照与站点配置；`src/content/blog/` 保存已经审核并可公开构建的文章。

`scripts/` 负责从 GitHub 获取或整理项目与贡献元数据、校验同步数据、生成审计报告、导入 HTML 候选和执行发布前审计。GitHub Actions 在 `main` 推送、定时任务或手动触发时运行同步、质量门、构建和 GitHub Pages 部署。站点没有数据库和后端写操作，任何访客可见更新都来自提交后的静态构建。

## 代码在哪里

- `src/pages/`：路由级页面。首页重点是 `src/pages/index.astro`；作品、写作、About、Contact、搜索和多语言路由应一起检查。
- `src/layouts/`：页面骨架、文章布局、元数据和全局导航装配。
- `src/components/`：可复用 UI。`components/projects/` 管理项目卡片与媒体，`components/lora/` 管理贡献和收藏体验，`components/layout/` 管理导航与页脚。
- `src/data/`：站点配置、项目与贡献同步快照、社交链接、当前关注方向与展示数据。
- `src/content/blog/`：已经审核、会被构建为公开文章的内容。
- `src/assets/`：项目封面、原创视觉资产和其他随构建优化的媒体。
- `scripts/`：同步、校验、报告、内容导入和发布审计脚本。
- `packages/pure/`：本仓库使用的 Astro Pure 主题源；不要把站点定制和上游主题改动混在同一个提交。
- `.github/workflows/`：GitHub Actions 的同步、构建和 GitHub Pages 部署流程。
- `content-drafts/review/`：被忽略的本地审核候选区；其中内容不应被当作已经发布。

## 品味与工程取舍

- 把复杂度留在边界：外部 API、GitHub 原始字段和导入格式在 `scripts/` 或数据整理层消化，不要泄漏到展示组件。
- 用解决当前问题的最小方案。没有真实需求就不要新增服务、框架、状态管理、分析追踪、重型动画库或运行时依赖。
- TypeScript 让编译器推断的地方就不要重复标注。不要使用 `any`；不可信的外部数据在进入快照前校验。
- 注释只说明意图、约束和不明显的原因，不复述代码动作。
- UI 不能撒谎。加载就表示确有加载，成功就表示底层动作已完成；不要做虚假进度、过期状态或不可恢复的乐观反馈。
- 优先复用已有的 Astro、原生浏览器 API、CSS、`IntersectionObserver`、Pointer Events 和主题组件模式。标准问题已有成熟实现时，不要为了“自己写”而重造。
- 如果本文件中的规则妨碍真实任务，说明冲突并等待维护者批准后再突破规则。

## 额外提醒

- 除非任务需要真实客户端验证或维护者明确要求，不要启动浏览器、开发服务器或视觉自动化。
- 新视觉资产由直接图像生成创建时，先写清楚用途、构图、安全区、风格、禁止元素和目标尺寸；不要凭一句“做得酷一点”生成一批无法使用的图片。
- 项目封面必须表达项目真实功能；文章配图必须服务文章内容；动漫/Favorites 的装饰资产不得复刻受版权保护的角色或官方画面。
- 修复问题时不要偷偷扩大任务。发现相邻优化项，记录下来，等当前改动通过验证后再处理。
